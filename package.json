{
  "name": "bambot",
  "version": "1.0.0",
  "description": "BambooHR Slack bot publishes company holidays, work anniversaries, first days, birthdays, and who is out each weekday.",
  "license": "MIT",
  "scripts": {
    "build": "serverless webpack",
    "deploy": "npm test && serverless deploy --verbose",
    "format": "prettier --write '{,!(.serverless|.webpack)/**/}*.+(js|json|md|ts|yml)'",
    "invoke": "ENVIRONMENT=local sls invoke local -f func",
    "lint": "tslint './**/*.ts' --exclude './node_modules/**/*.ts' --fix",
    "test": "npm run lint && jest",
    "watch": "npm test -- --watch"
  },
  "dependencies": {
    "@therockstorm/utils": "^3.0.7",
    "axios": "^0.18.0",
    "cwait": "^1.1.2",
    "dayjs": "^1.8.14",
    "source-map-support": "^0.5.12",
    "xml2js-es6-promise": "^1.1.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.25",
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.1",
    "husky": "^2.2.0",
    "jest": "^24.8.0",
    "lint-staged": "^8.1.6",
    "lodash": ">=4.17.11",
    "merge": ">=1.2.1",
    "prettier": "^1.17.1",
    "serverless": "^1.42.3",
    "serverless-webpack": "^5.3.0",
    "ts-jest": "^24.0.2",
    "ts-loader": "^6.0.0",
    "tslint": "^5.16.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.4.5",
    "webpack": "^4.31.0",
    "webpack-node-externals": "^1.7.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts}": [
      "tslint --fix"
    ],
    "*.{js,json,md,ts,yml}": [
      "prettier --write",
      "git add"
    ]
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
